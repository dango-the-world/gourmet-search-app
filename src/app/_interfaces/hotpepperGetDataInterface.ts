export interface hotpepperGetData {
  results: {
    shop: {
      id: string;
      name: string;
      access: string;
      pc: {
        l: string;
      };
      logo_image: string;
    }[];
  };
}
